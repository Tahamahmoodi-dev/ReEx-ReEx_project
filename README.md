# ReEx-ReEx_project
ReEx - AeEx is a high_performance C tool for Linux (Kali) that compares two directories, highlights missing ([×]) and added ([✓]) files in color, and can optionally extract added files with paths. Perfect for reverse_engineering, audits, and file version analysis
